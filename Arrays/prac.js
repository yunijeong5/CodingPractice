//Array.splice(index, number of items to delete including the index, items to add before the index)
const arr = [1, 2, 3]
arr.splice(1, 3) 
console.log(arr)

const arr2 = [1, 2, 3]
arr2.splice(1, 0, 4, 5)

console.log(arr2)